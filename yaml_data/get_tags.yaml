-
  name: 获取微信标签接口
  request:
    method: get
    url: https://api.weixin.qq.com/cgi-bin/tags/get
  validate:
  extract:
    # access_token: '"access_token":"(*)"'
    # name: '"name":"(.*?)"'
    ss: ${read_yaml(access_token)}